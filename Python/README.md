# Snake python

Snake Python
You can use the Python version.
You will need python and pygame to play.

Dependencies:
Install pygame with

```bash
pip install pygame
```
Or
```bash
apt install python3-pygame
```
(for Debian)